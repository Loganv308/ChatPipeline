services:
  chatpipeline:
    build: .
    restart: always
    env_file: .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"   # rotate logs at 10MB
        max-file: "3"     # keep last 3 log files